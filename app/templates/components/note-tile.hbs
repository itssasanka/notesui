<div class="note">
	<div class="title">
		{{m.title}}
	</div>
	<div class="body">
		{{m.content}}
	</div>
	{{#each m.tags as |tag|}}
		{{#link-to "notes" (query-params tag_id=tag.id) class='button-link' replace=true}}
			{{tag.name}}
		{{/link-to}}
	{{/each}}
</div>


{{yield}}
